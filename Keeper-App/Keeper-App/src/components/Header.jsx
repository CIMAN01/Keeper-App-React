import React from "react";
import CreateIcon from '@material-ui/icons/Create';

function Header() {
    return (
    <header>
        <h1><CreateIcon />Keeper App</h1>
        <p>(a fun way to create digital sticky notes)</p>
    </header> 
    );
}

export default Header;